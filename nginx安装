nginx 安装


wget http://nginx.org/download/nginx-1.10.1.tar.gz //下载nginx

tar -xzvf nginx-1.10.1.tar.gz -c /usr/local/vendor //解压文件到指定目录

安装 pcre，zlib库

./configure --prefix=/usr/local/nginx  //编译配置文件  

make && make install
